<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.easy.mallpositioning.mapper.LocationDataMapper">


    <select id="selectAllByDeviceIdList" resultType="java.lang.String">
        select d.device_id from monitor_tracked_mapping as m
        join devices as d on m.tracked_device_id=d.id where m.monitor_id=#{id}
    </select>
</mapper>
